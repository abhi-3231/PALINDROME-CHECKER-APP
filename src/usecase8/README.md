# Use Case 8 – LinkedList Based Palindrome Checker

## Description
This use case checks whether a string is a palindrome using a **LinkedList**.

Characters from the input string are stored inside a LinkedList.

Then characters are compared by removing:
- the first element
- the last element

This demonstrates how LinkedList supports efficient double-ended operations.
